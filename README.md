# REFramework Chinese Builder

此仓库保存 REFramework 汉化构建脚本和词库。

GitHub Actions 每天检查一次；距离上次成功发布满 72 小时时，使用 Windows Visual Studio 2022 环境构建 ZIP，并在 Releases 页面保留历史版本。

本地手动构建入口：

```powershell
python build_chinese.py
```
